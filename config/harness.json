{
  "profiles": {
    "production": {
      "description": "产线默认档案：图像对齐、颜色和本地确定性规则。场景中的 VLM 与 YOLO 由各自配置直接调用。",
      "enabled_plugins": [
        "opencv_rules"
      ],
      "capability_routes": {
        "image.color": "opencv_rules",
        "image.alignment": "opencv_rules",
        "image.quality": "opencv_rules"
      }
    },
    "minimal": {
      "description": "故障排查档案：仅保留本地颜色、对齐和质量规则。",
      "enabled_plugins": [
        "opencv_rules"
      ],
      "capability_routes": {
        "image.color": "opencv_rules",
        "image.alignment": "opencv_rules",
        "image.quality": "opencv_rules"
      }
    },
    "configuration": {
      "description": "场景配置档案：启用本地规则；VLM 和已发布 YOLO 由场景节点直接选择。",
      "enabled_plugins": [
        "opencv_rules"
      ],
      "capability_routes": {
        "image.color": "opencv_rules",
        "image.alignment": "opencv_rules",
        "image.quality": "opencv_rules"
      }
    }
  }
}
